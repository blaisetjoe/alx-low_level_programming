#include "main.h"

/**
*_abs - computes the absolute of an intenger
*@n: the intenger to be computed
*
* Return: the absolute value of the intenger.
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
